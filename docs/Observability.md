# Observability

Structured logs, metrics, tracing, health endpoints, sync statistics.

Distinct from the operational logs above, DevPit exposes a *user-facing*
sync/poll log in the UI (per-cycle summaries, per-call detail on failure)
so users can see polling working and understand failures. See
docs/Design_Decisions.md §16.
